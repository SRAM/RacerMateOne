On the first run it will create a test.ini file. 

Close the program and then open the test.ini file and set the appropriate com port for the CT.  Save that and then run main.exe again.  It will connect to the CT and start streaming data. 

The first Column is the time, 2nd is watts, 3rd is DF, 4-6 are TSS/IF/NP.

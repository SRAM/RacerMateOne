<UserControl x:Class="RacerMateOne.Controls.ColorBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="15" Width="15"
	Name="btn" Loaded="btn_Loaded">
	<UserControl.Triggers>
		<EventTrigger RoutedEvent="UserControl.MouseEnter">
			<BeginStoryboard>
				<Storyboard>
					<DoubleAnimation
                        Storyboard.TargetName="Highlight"
                        Storyboard.TargetProperty="Opacity"
                        To="1" Duration="0:0:0.1"
                        AutoReverse="False" RepeatBehavior="1x"
                     />
				</Storyboard>
			</BeginStoryboard>
		</EventTrigger>
		<EventTrigger RoutedEvent="UserControl.MouseLeave">
			<BeginStoryboard>
				<Storyboard>
					<DoubleAnimation
                        Storyboard.TargetName="Highlight"
                        Storyboard.TargetProperty="Opacity"
                        To="0" Duration="0:0:0.1"
                        AutoReverse="False" RepeatBehavior="1x"
                     />
				</Storyboard>
			</BeginStoryboard>
		</EventTrigger>
	</UserControl.Triggers>
	<Border Name="Bdr" Background="#FF999999" BorderBrush="White" BorderThickness="1" MouseDown="Border_MouseDown">
		<Border.BitmapEffect>
			<OuterGlowBitmapEffect x:Name="Highlight" Opacity="0" GlowColor="White" GlowSize="5"/>
		</Border.BitmapEffect>
	</Border>
</UserControl>

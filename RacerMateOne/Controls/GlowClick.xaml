<c:BaseClick x:Class="RacerMateOne.Controls.GlowClick"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:c="clr-namespace:RacerMateOne.Controls"
	>
	<Border Name="PartnerOverlay" Background="White" Opacity="0.0" CornerRadius="30">
		<Border.Effect>
			<BlurEffect Radius="6"/>
		</Border.Effect>
		<Border.Triggers>
			<EventTrigger RoutedEvent="Border.MouseEnter">
				<BeginStoryboard>
					<Storyboard>
						<DoubleAnimation
											Storyboard.TargetName="PartnerOverlay" 
											Storyboard.TargetProperty="Opacity"
											To="0.5" Duration="0:0:0.3" 
											/>
					</Storyboard>
				</BeginStoryboard>
			</EventTrigger>
			<EventTrigger RoutedEvent="Border.MouseLeave">
				<BeginStoryboard>
					<Storyboard>
						<DoubleAnimation
											Storyboard.TargetName="PartnerOverlay" 
											Storyboard.TargetProperty="Opacity"
											To="0.0" Duration="0:0:0.3" 
											/>
					</Storyboard>
				</BeginStoryboard>
			</EventTrigger>
		</Border.Triggers>
	</Border>
</c:BaseClick>
